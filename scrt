for i = 1, 1000000 do
  local part = Instance.new("Part")
  part.Parent = workspace
  part.Position = Vector3.new(math.random(-500, 500), 10, math.random(-500, 500))
  wait(0.1) -- Wait 0.1 seconds before spawning the next part
end
